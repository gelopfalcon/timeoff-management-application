pipeline {
    agent any
    stages {
        stage('Build project') {
            steps {
                echo "Step 1: Installing dependencies"
                sh 'npm ci'
                echo "Step 2: Running Test"
         
            }
        }
    }
}