pipeline {
    agent any
    stages {
        stage('Build project') {
            steps {
                echo "Step 1: Installing dependencies"
                sh 'npm install'
                echo "Step 2: Running Test"
         
            }
        }
    }
}